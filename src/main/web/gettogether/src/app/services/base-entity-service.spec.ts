import { BaseEntityService } from './base-entity-service';

describe('BaseEntityService', () => {
  it('should create an instance', () => {
    expect(new BaseEntityService()).toBeTruthy();
  });
});
